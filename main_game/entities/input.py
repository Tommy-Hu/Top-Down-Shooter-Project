# Input package is what the input is stored in
class input_package:
    def __init__(self, x, y, ks, mbs, pos):
        self.move_X = x
        self.move_Y = y
        self.keys = ks
        self.mouse_buttons = mbs
        self.mouse_pos = pos
